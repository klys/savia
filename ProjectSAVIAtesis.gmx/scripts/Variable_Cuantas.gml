/* Variable_Cuantas()

    Devuelve la cantidad de variables actuales en el grafo.
    
    Retorna:
            Valor Real
            -1 si no hay variables
            0 indica q solo hay una sola variable
*/

with(objGrafoControl)
{
    return vars;
}
