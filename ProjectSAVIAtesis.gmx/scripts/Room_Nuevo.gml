/* Room_Nuevo(RoomName)

    Crea un nuevo Cuarto y le asigna un nombre

    Parametros:
            arg 0 - RoomName - Nombre del cuarto a crear

    Retorna:
            El id del cuarto creado
*/

/*
with(objRoomManager)
{
    var NewRoom;
    NewRoom = room_duplicate(room0)
    //room_assign(NewRoom, room0);
    ds_list_add(cuartos, NewRoom);
    //room = NewRoom;
    room_goto(NewRoom)
    room_caption = argument0
    Console_TextAdd("Nuevo Grafo: "+room_get_name(NewRoom))
    objGrafoControl.GrafoName = argument0
    // FALTA ASIGNACION DEL NOMBRE DEL ROOM
}
*/
