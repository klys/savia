/* Actividad_Existe(nombre)
    Script que verifica la existencia de una actividad
    por su nombre.
    
    Retorna: boolean
*/

with(objActividad)
{
    if (strNombre == argument0) return true;
}
return false;
